"""Генерация финального отчёта: рендереры (.md/.docx/.txt).

Точки входа:

* ``report.markdown.render_markdown(data) -> str`` — Markdown.
* ``report.plain.render_plain(data) -> str`` — plain text без разметки.
* ``report.docx_render.write_docx(data, path)`` — DOCX через python-docx.

Все рендереры принимают единый dict-формат (результат
``modes.synthesize.run()``). Шесть секций отчёта:

1. Краткое изложение отклонения
2. Установленные факты (по ВНД)
3. Анализ отклонения
4. Релевантные фрагменты ВНД (валидированные цитаты)
5. Итоговая классификация
6. Рекомендуемая усиленная формулировка

Пустые секции рендерятся плейсхолдером ``*(секция не заполнена: <причина>)*``.
"""
